version: '3.1'

services:
  # Test application
#  php:
#    image: php:apache
#    ports:
#      - "8080:80"
#    volumes:
#      - ./serve/:/var/www/html/

  db:
    image: postgres:16.0
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: example
      POSTGRES_DB: captive-portal

  redis:
    image: redis:latest
    restart: always
    ports:
      - "6379:6379"
    environment:
      - REDIS_PORT=6379
